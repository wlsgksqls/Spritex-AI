# Pages (frontend) — inlined at `next build`. Not a secret.
NEXT_PUBLIC_API_BASE=http://127.0.0.1:8787

# Optional. Local Next does not call Gemini; the Worker does.
# Put the real key on the Worker: worker/.dev.vars or Cloudflare Worker secrets.
GEMINI_API_KEY=
