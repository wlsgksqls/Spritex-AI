# Optional default key. Users can still paste their own key in the studio UI.
# User-provided keys are sent only as the x-gemini-api-key request header and are not stored on the server.
GEMINI_API_KEY=
